import { combineReducers } from 'redux';
import { messages } from './send_message_reducer';

export default combineReducers({
    // auth
    messages,
    // product
    // form
    // form: formReducer
});